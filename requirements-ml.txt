# requirements-ml.txt — stack OPCIONAL de TM semântica (RAG nº1). Não é requerida pelo runtime
# determinístico: sem ela o context_pack cai p/ [] (fallback testado). Instalada e auditada por
# CVE em quality.yml (job deps, #89) — nunca instalada em test.yml (nenhum teste funcional
# depende dela).
#
# Instala SÓ quando for LIGAR a busca semântica num projeto com `db` (hoje: translation_software):
#   pip install -r requirements-ml.txt
#   python framework/cli.py db index projects/translation_software/translation_software.db bof4
#
# Pesado (~700 MB–1,5 GB): puxa torch + baixa o modelo MiniLM (~470 MB) no cache do HuggingFace
# (~/.cache/huggingface). Os vetores ficam na própria <projeto>.db (tabela tm_embeddings + vec0).
#
# GPU: o torch default é CPU. Aceleração AMD via ROCm é Linux-only; no Windows fica CPU (ok p/
# ~6k linhas, poucos minutos) — ou instale um torch-directml ANTES, à parte.
#
# #89: 1º install real feito (2026-07-05) — floors travados em ~= (compatible release, mesma
# política do requirements-dev.txt) nas versões confirmadas nesse install. torch NÃO é pinado
# aqui: é dependência TRANSITIVA de sentence-transformers (resolveu 2.12.1), não direta —
# pinar direto mudaria o contrato do arquivo sem necessidade; pip-audit audita a árvore inteira
# (inclui torch) mesmo sem pin direto.

sentence-transformers~=5.6.0     # encoder paraphrase-multilingual-MiniLM-L12-v2 (dim 384)
sqlite-vec~=0.1.9              # índice vetorial (vec0) dentro do SQLite
flashrank~=0.2.10              # reranker ms-marco-MiniLM-L-12-v2 (opcional, melhora o top-k)
