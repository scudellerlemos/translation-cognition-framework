# Copie este arquivo para `.env` (na raiz do framework) e preencha com sua chave real.
# O `.env` esta no .gitignore — NUNCA sera commitado. Nao cole a chave em chat/PR.
# O harness (framework/runtime/model.py) carrega este arquivo automaticamente no backend `api`.

ANTHROPIC_API_KEY=sk-ant-cole-sua-chave-aqui
